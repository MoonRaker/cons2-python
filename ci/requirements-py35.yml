name: test_env
channels:
    - conda-forge
    - defaults
dependencies:
    - python=3.5
    - numpy
    - scipy
    - pandas
    - pytest
    - pytest-cov
    - nose
    - pip:
        - coveralls
        - pytest-timeout
